$ssid = "Mitu"

netsh wlan delete profile name="$ssid" *>$null

$ssidHex = ($ssid.ToCharArray() | ForEach-Object { [System.Convert]::ToString([int]$_, 16) }) -join ""

$xmlProfile = @"
<?xml version="1.0"?>
<WLANProfile xmlns="http://microsoft.com">
    <name>$ssid</name>
    <SSIDConfig>
        <SSID>
            <hex>$ssidHex</hex>
            <name>$ssid</name>
        </SSID>
    </SSIDConfig>
    <connectionType>ESS</connectionType>
    <connectionMode>auto</connectionMode>
    <MSM>
        <security>
            <authEncryption>
                <authentication>open</authentication>
                <encryption>none</encryption>
                <useOneX>false</useOneX>
            </authEncryption>
        </security>
    </MSM>
</WLANProfile>
"@

$profilePath = "$env:TEMP\freewifitrustmebro.xml"
[System.IO.File]::WriteAllText($profilePath, $xmlProfile, [System.Text.Encoding]::UTF8)

netsh wlan add profile filename=$profilePath

netsh wlan connect name=$ssid

Remove-Item -Path $profilePath

